module earthvm_nesting

  ! Module to handle nests.

  use earthvm_model, only: earthvm_model_type
  
  implicit none
  
  type(earthvm_model_type), allocatable :: nests(:)

  contains

end module earthvm_nesting
